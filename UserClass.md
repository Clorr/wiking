The User class is used to store user information.

User is ALWAYS identified.

By default, it is a 'guest' profile (only with 'show' permission).

User class is also useful to store misc session-related informations.